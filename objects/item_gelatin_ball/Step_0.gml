/// @description
/*
*/

// Inherit the parent event
event_inherited();

if (collected = false && collect)
	{
	playerMAXHP += 1;
	playerDAMAGE +=1;
	playerHP +=1;
	image_alpha = 1;
	collected = true;
	}